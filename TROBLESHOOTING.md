# 🔧 РЕШЕНИЕ ПРОБЛЕМ

Этот гайд поможет решить типичные проблемы при запуске и работе бота.

## Проблема 1: Ошибка при установке библиотек

### Симптом:
```
ERROR: Could not find a version that satisfies the requirement...
```

### Решение:
1. Обновите pip:
```bash
python -m pip install --upgrade pip
```

2. Попробуйте установить библиотеки по одной:
```bash
pip install python-telegram-bot
pip install groq
pip install python-dotenv
pip install beautifulsoup4
pip install requests
pip install Pillow
```

3. Если ошибка продолжается, укажите флаг --user:
```bash
pip install --user -r requirements.txt
```

## Проблема 2: "ModuleNotFoundError: No module named 'telegram'"

### Симптом:
При запуске бота появляется ошибка о том, что модуль не найден.

### Решение:
1. Убедитесь, что вы установили библиотеки:
```bash
pip install python-telegram-bot
```

2. Проверьте, что используете правильную версию Python:
```bash
python --version
```
Должна быть 3.8 или выше.

3. Если у вас установлено несколько версий Python, попробуйте:
```bash
python3 bot.py
```
или
```bash
py bot.py
```

## Проблема 3: Бот не отвечает в Telegram

### Симптом:
Бот запущен, но не отвечает на сообщения в Telegram.

### Решение:
1. Проверьте, что токен в .env файле правильный:
   - Откройте .env
   - Убедитесь, что нет пробелов вокруг знака =
   - Правильно: `TELEGRAM_BOT_TOKEN=1234:ABC`
   - Неправильно: `TELEGRAM_BOT_TOKEN = 1234:ABC`

2. Проверьте, что файл .env находится в той же папке, что и bot.py

3. Попробуйте получить новый токен от @BotFather:
   - Отправьте `/mybots`
   - Выберите своего бота
   - API Token → Revoke current token
   - Скопируйте новый токен в .env

4. Перезапустите бота (Ctrl+C, затем `python bot.py`)

## Проблема 4: "Error: No such file or directory: '.env'"

### Симптом:
Бот не может найти файл .env

### Решение:
1. Убедитесь, что файл .env находится в той же папке, что и bot.py

2. Проверьте имя файла - должно быть именно `.env` (с точкой в начале)

3. В Windows файл может быть скрыт. Включите показ скрытых файлов:
   - Проводник → Вид → Показать → Скрытые элементы

4. Если проблема не решается, создайте файл заново:
   - В VS Code: File → New File
   - Сохраните как `.env` (с точкой)
   - Добавьте токены

## Проблема 5: "groq.AuthenticationError"

### Симптом:
```
groq.AuthenticationError: Invalid API Key
```

### Решение:
1. Проверьте правильность GROQ_API_KEY в файле .env

2. Ключ должен начинаться с `gsk_`

3. Получите новый ключ:
   - Перейдите на https://console.groq.com
   - API Keys → Create API Key
   - Скопируйте новый ключ в .env

4. Убедитесь, что в ключе нет пробелов в начале или конце

## Проблема 6: "Rate limit exceeded"

### Симптом:
```
Rate limit exceeded. Please try again later.
```

### Решение:
1. Это означает, что вы превысили лимит запросов (30 в минуту)

2. Подождите 1-2 минуты и попробуйте снова

3. Для презентации используйте бота умеренно

4. В будущем можно:
   - Перейти на платный план Groq
   - Использовать другую ИИ-модель
   - Добавить кэширование ответов

## Проблема 7: Бот работает, но очень медленно отвечает

### Симптом:
Бот долго думает перед ответом (больше 10 секунд)

### Решение:
1. Это нормально для сложных запросов

2. Убедитесь, что у вас стабильный интернет

3. Модель Llama 3.1 70B - мощная, но может быть медленной

4. Можно изменить модель на более быструю:
   В файле bot.py найдите строку:
   ```python
   model="llama-3.1-70b-versatile"
   ```
   Замените на:
   ```python
   model="llama-3.1-8b-instant"
   ```
   (быстрее, но менее умная)

## Проблема 8: "Connection Error" или "Network Error"

### Симптом:
Ошибки соединения с интернетом

### Решение:
1. Проверьте интернет-соединение

2. Проверьте, не блокирует ли антивирус или файрвол Python

3. Если вы за прокси или VPN:
   - Убедитесь, что Telegram и Groq доступны
   - Попробуйте отключить VPN временно

4. В некоторых странах Telegram может быть заблокирован - используйте VPN

## Проблема 9: Бот запускается и сразу останавливается

### Симптом:
После команды `python bot.py` программа сразу завершается

### Решение:
1. Посмотрите на сообщения об ошибках в терминале

2. Проверьте, что все обязательные переменные заполнены в .env:
   - TELEGRAM_BOT_TOKEN
   - GROQ_API_KEY

3. Добавьте в начало bot.py для отладки:
   ```python
   print("Бот загружается...")
   print(f"Token: {os.getenv('TELEGRAM_BOT_TOKEN')[:10]}...")
   print(f"Groq: {os.getenv('GROQ_API_KEY')[:10]}...")
   ```

4. Запустите бота с подробным выводом:
   ```bash
   python bot.py --verbose
   ```

## Проблема 10: "Pillow" не устанавливается на Windows

### Симптом:
Ошибка при установке библиотеки Pillow

### Решение:
1. Установите через pip с флагом --upgrade:
```bash
pip install --upgrade Pillow
```

2. Если не помогает, скачайте wheel файл:
   - Перейдите на https://www.lfd.uci.edu/~gohlke/pythonlibs/#pillow
   - Скачайте подходящую версию для вашей системы
   - Установите: `pip install имя_файла.whl`

## Проблема 11: Бот не может анализировать сайты

### Симптом:
Ошибка при отправке ссылки на сайт

### Решение:
1. Некоторые сайты блокируют автоматический парсинг

2. Это нормальное поведение для защищенных сайтов

3. Попробуйте другие сайты для демонстрации

4. Бот всё равно даст полезные рекомендации на основе URL

## Общие рекомендации

### Перед запуском всегда проверяйте:
✅ Python 3.8+ установлен
✅ Все библиотеки установлены (`pip list`)
✅ Файл .env в правильной папке
✅ Токены в .env корректны
✅ Интернет работает стабильно

### Для отладки используйте:
```bash
python -v bot.py  # Подробный вывод
python -u bot.py  # Unbuffered вывод
```

### Полезные команды для диагностики:
```bash
python --version          # Проверить версию Python
pip list                  # Список установленных пакетов
pip show python-telegram-bot  # Информация о пакете
where python             # Где установлен Python (Windows)
which python             # Где установлен Python (Mac/Linux)
```

## 🆘 Если ничего не помогло

1. Удалите все библиотеки и установите заново:
```bash
pip uninstall -y python-telegram-bot groq python-dotenv beautifulsoup4 requests Pillow
pip install -r requirements.txt
```

2. Используйте виртуальное окружение:
```bash
python -m venv venv
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate
pip install -r requirements.txt
```

3. Переустановите Python:
   - Скачайте последнюю версию с python.org
   - При установке отметьте "Add Python to PATH"
   - Перезагрузите компьютер

4. Проверьте антивирус - он может блокировать Python

5. Попросите помощи:
   - Скопируйте текст ошибки полностью
   - Напишите в какой момент возникла проблема
   - Укажите версию Python и ОС

## 📞 Контакты для поддержки

Если проблема критическая и не решается:
- Проверьте GitHub Issues (если код на GitHub)
- Посмотрите документацию библиотек
- Задайте вопрос на Stack Overflow
